import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paybill.app',
  appName: 'paybill',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
